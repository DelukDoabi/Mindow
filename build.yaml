targets:
  $default:
    builders:
      json_serializable:
        options:
          # Single, project-wide field naming policy: JSON uses snake_case.
          field_rename: snake
          explicit_to_json: true
          create_to_json: true
          create_factory: true
